{
    "$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
    "description": "Schema for a basket action event",
    "self": {
        "vendor": "test.example.iglu",
        "name": "basket_action_event",
        "format": "jsonschema",
        "version": "1-0-0"
    },
    "type": "object",
    "properties": {
        "action": {
            "description": "The type of action that changed the basket state",
            "type": "string",
            "enum": ["add", "remove"]
        }
    },
    "required": ["action"],
    "additionalProperties": false
}
